import styled from 'styled-components';

const SettingsWrapper = styled.div`
  padding: 50px;
  display: flex;
  flex-direction: column;
`;

const StyledTitle = styled.h1`
  font-weight: normal;
  font-size: 35px;
`;

const StyledLabel = styled.span`
  font-size: 17px;
  margin: 30px 0 5px;
`;

export { SettingsWrapper, StyledTitle, StyledLabel };
